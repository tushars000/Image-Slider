Website using flask python framework

A simple Website which register users and allow them to login with the same credentials only if the credentials match with the database.
  
libraries used and their function:-
1)
